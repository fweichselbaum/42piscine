cat /etc/passwd | grep --line-regexp --invert-match "#.*" | awk "NR % 2 == 0" | awk -F: '{ print $1; }' | rev | sort -r | awk "NR >= $FT_LINE1 && NR <= $FT_LINE2" | awk '{ print $0; }' ORS=', ' | sed "s/, $/./"
